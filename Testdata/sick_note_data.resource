*** Settings ***
Documentation    This file contains the dictionaries for Sick Note (Medical Certificate) test data used in the Diary - Add Sick note test case.
Resource    ../common/super.resource

*** Variables ***
# No direct user input fields (text, file upload, etc.) are specified in the test case steps for Sick Note creation.
# Therefore, this dictionary is intentionally left empty as per test data generation rules.
&{SICK_NOTE_DATA}
